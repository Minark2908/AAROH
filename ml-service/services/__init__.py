"""
Local services package for the `ml-service` FastAPI app.

Having this file makes `services/` a proper Python package, so imports like
`from services.iot_service import get_sensor_data` work consistently.
"""

